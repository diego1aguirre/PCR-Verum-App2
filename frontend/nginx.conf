server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass ${VITE_API_URL}/;
    }

    location /flask/ {
        proxy_pass ${VITE_FLASK_URL}/;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
